node_modules/
bazel-*
.bazelrc.user
dist/
release/
/data/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
.env.worktree
.DS_Store
Thumbs.db
MODULE.bazel.lock

# Local scratch output (Arabic probe bins, etc.)
/tmp/

# Open-orders KOT bake emit script (base64 buffers; regenerate locally)
scripts/printing/kitchen/out/

# Repo-local Rust toolchain (must not commit)
/.cargo/
/.rustup/

# Rust build artifacts
native/win_rawprint/target/

# Android build artifacts
apps/android/**/build/
apps/android/.gradle/
apps/android/.kotlin/
apps/android/local.properties
