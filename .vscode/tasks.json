{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile shared & db packages for ts-node",
      "detail": "Runs tsc in packages/shared and packages/db to emit .js/.d.ts files needed by ts-node (VSCode launch configs).",
      "type": "shell",
      "command": "pnpm exec tsc -p packages/shared/tsconfig.json && pnpm exec tsc -p packages/db/tsconfig.json",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": ["$tsc"]
    }
  ]
}
